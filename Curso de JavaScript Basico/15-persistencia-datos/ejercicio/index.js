const nom = "Flako"
const ape = "BB"

const persona = {nombre: nom, apellido: ape}

// localStorage.setItem("personaLocal", JSON.stringify(persona))

// sessionStorage.setItem("personaSession", JSON.stringify(persona))

// document.cookie = `personaCookie=${JSON.stringify(persona)};expires=${new Date().setMinutes(new Date().getMinutes()+2)}`